# Copy to .env.local if you need to point the dev proxy at a different backend.
# The default (http://127.0.0.1:8000) works with `uvicorn app.main:app --reload`.
VITE_BACKEND_URL=http://127.0.0.1:8000
